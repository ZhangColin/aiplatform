-- ========================================================================
-- base.skills：技能库操作者留痕（#246-T2/#248）——skl_skills 补 operator 两列
--
-- operator 两列＝后台管理动作操作者（admin 侧管理员 TSID＋昵称，非平台用户，
-- #151 口径，与 V5 knw_materials / V6 ord_price_entries.operator 同款）：Id 供
-- 关联、Name 供直读（账号删除后仍可读）；VARCHAR 存外域标识，不与平台
-- accountId 混型；服务端不校验真实性（签名面担保调用应用，操作者头明示信任）。
--
-- 落痕口径（#248「全程操作者留痕」）：安装＝装者落 created 侧、启停＝最近
-- 动作者（重复动作幂等、操作者留最近一次，知识域先例）；卸载无行可留不留痕
-- （全局审计流水不建——admin 侧自有操作日志，知识删除先例）。T1 只读侧未落
-- 两列，存量行（仅测试夹具）不回填（呈现为空）。
-- ========================================================================

ALTER TABLE skl_skills
    ADD COLUMN operator_id   VARCHAR(64),
    ADD COLUMN operator_name VARCHAR(200);
