-- ========================================================================
-- base.skills：技能条目 scripts/ 资源列（#253 安装内容面补课，ADR-0021）
--
-- 内容面收知识/规范（frontmatter＋正文）＋脚本（scripts/ 子树）——V15 注释
-- 预留的话头（「scripts/ 资源表属 T2 安装票，本表不预留」）在此兑现，拍板
-- 落「内容面扩展」而非资源表：resources 与框架 AgentSkill.resources 同形
-- （路径 → 内容 map），条目行仍是唯一管理单元——插入随快照带、显式更新
-- 原地翻新刷、卸载行删即净，更新/卸载/版本留痕与 SKILL.md 条目天然同轨，
-- 无新增读写面。
--
-- 值口径：键＝技能目录相对路径（scripts/…，正斜杠）；值＝文本内容（非
-- UTF-8 按框架 base64: 前缀约定）。存量行无 scripts 概念——DEFAULT '{}'
-- 回填即「空资源面」，与「纯 Markdown 技能」同语义。
-- ========================================================================

ALTER TABLE skl_skills ADD COLUMN resources JSONB NOT NULL DEFAULT '{}';
