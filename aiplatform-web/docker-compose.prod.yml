services:
  app:
    build: .
    container_name: aiplatform-web
    restart: unless-stopped
    ports:
      - "3333:3333"
    env_file:
      - .env.production
    networks:
      - webnet
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3333/"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

networks:
  webnet:
    external: true
