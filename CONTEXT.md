# AI 开发平台

面向非技术用户的定制系统交易平台：用户以一句话需求进入，平台访谈梳理出 PRD、在沙箱中生成可操作的系统实现，确认后下单、后台报价、支付。平台止于支付完成。

## Language

**系统**:
用户定制的目标系统本身。沙箱中生成的高保真可操作实现即系统的当前形态——用户看到、可操作的就是未来系统的样子与功能。
_Avoid_（用户可见面）: Demo、原型、样品、演示版

**指令区**:
项目界面左侧全程常开的对话区，用户在此提意见、发表看法；PRD 与系统的更新都由指令区的输入发起。
_Avoid_: 聊天窗口、消息框

**BA**:
平台的需求分析师智能体：以每轮一问的访谈引导用户把模糊想法梳理成 PRD，并负责其后续修订。

**PRD**:
平台替用户梳理的需求文档，面向非技术用户可读，固定七章节（需求背景、目标用户、核心场景、范围边界、关键约束、功能清单、待定项）。单最新版，由 BA 撰写修订，用户不直接编辑。
_Avoid_: 需求文档、规格书

**功能清单**:
PRD 的章节：编号列出系统的页面/功能点及每点的验收要点，是系统生成的直接依据。

**待定项**:
PRD 中仍缺用户确认信息的需求点；催促收敛或信息不足时缺口记录于此。

**定稿**:
用户以显式确认动作（「开始做系统」）宣告 PRD 稳定、进入系统生成的时点。
_Avoid_（用户可见面）: 确认门、G1

**生成**:
定稿后平台自动在沙箱中起首个编码过程、产出系统初始形态；无需用户额外操作。用户侧表述「正在为您生成系统」。
_Avoid_（用户可见面）: 开发、构建、出 Demo

**迭代**:
定稿后「用户在指令区提意见 → BA 受理判定 →（需求变更先改 PRD）→ 修正过程 → 系统更新」的循环，每一轮称一轮迭代。无次数上限，意见发散时 BA 催促收敛。
_Avoid_: 期、轮次回归

**确认下单**:
用户宣告系统满意、进入下单流程的显式按钮动作；首次生成完成后常驻指令区/侧栏，允许零迭代直接确认。
_Avoid_（用户可见面）: 验收、确认门、G2

**工作区**:
一个项目唯一的持久化根：静态文件、docker 文件映射、系统文件与数据的全部落点；数据目录映射其中，容器可随时重建而不丢数据。面向用户不直接暴露此词。
_Avoid_: 沙箱目录、项目空间
