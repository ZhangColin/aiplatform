-- ========================================================================
-- base.workspace：预览网关化——工作区不再随机映射宿主端口（#128）
-- 预览走子域 {id}.localhost、经平台 nginx 网关路由到容器 ws-{id}:8081。
-- preview_port 列为旧随机端口暴露机制的残留，随概念出局删除——预览 URL 是
-- workspaceId 的纯函数，不落库。
-- ========================================================================
ALTER TABLE wsp_workspaces DROP COLUMN preview_port;
