# AI 开发平台

面向非技术用户的定制系统交易平台：用户以一句话需求进入，平台访谈梳理出 PRD、在沙箱中生成可操作的系统实现，确认后下单、后台报价、支付。平台止于支付完成。

## 业务语言

**项目**:
用户一次定制需求的全程载体：从一句话需求创建，承载对话区会话、PRD、系统与订单；支付完成归档即终点，归档后再来为新项目。
_Avoid_: 二期（归档后新需求开新项目，不续用）、工单

**系统**:
用户定制的目标系统本身。沙箱中生成的高保真可操作实现即系统的当前形态——用户看到、可操作的就是未来系统的样子与功能。
_Avoid_（用户可见面）: Demo、原型、样品、演示版

**对话区**:
项目界面左侧全程常开的对话区，用户在此提意见、咨询项目情况、发表看法；PRD 与系统的更新都由对话发起——动作融入对话流，不设「开始做系统」式专设按钮（主智能体判断需求收敛时在对话区确认推进，或用户主动提出）。输入由平台三分类派发（意见 / 咨询 / 兜底），对用户隐式。构建 run 的工作过程呈现也长在对话区：run 进行中呈现为一条生长中的工作消息（用户语言解说 + 动作状态卡，步骤分组；思考与代码不播），run 收口定格为收尾卡（本轮摘要与轮末统计），动作明细不常驻。
_Avoid_: 聊天窗口、消息框、指令区（旧称，已出局）、Bot 区（用户俚称，不入册）

**主智能体**:
平台与用户对话的唯一职能体：以每轮一问的追问梳理需求（问答卡发起）、撰写修订 PRD（挂载 PRD 写作技能）、答询项目情况（咨询零产物，查不到就答不知）、受理迭代意见并做需求侧判定（要不要追问、要不要改 PRD），据此发起或排队构建 run。永不读写沙箱代码——代码变更全部发生在 run 内。
_Avoid_: BA、需求分析师、助理（旧职能体词条已出局，能力并入本词条）、角色标签（用户面不分角色呈现）

**问答**:
智能体运行中向用户发起的提问（单选/多选/自由输入），答复即续跑；以对话区内的问答卡呈现。
_Avoid_: 等待点、HITL、转任务（旧机制已出局）

**挂起问答**:
已发出、尚待用户答复的问答。挂起期间运行暂停、答复即续跑；对话区的输入此时即作答。
_Avoid_: 悬置问题、待确认工具调用（引擎层旧词）

**权限确认**:
run 执行中需要用户批准的工具操作（如危险命令），以对话区内的确认卡呈现，批准或拒绝即续跑。
_Avoid_: 审批、HITL

**PRD**:
平台替用户梳理的需求文档，面向非技术用户可读，固定七章节（需求背景、目标用户、核心场景、范围边界、关键约束、功能清单、待定项）。单最新版，由主智能体撰写修订（模板与写法由 PRD 写作技能承载），用户不直接编辑。
_Avoid_: 需求文档、规格书

**功能清单**:
PRD 的章节：编号列出系统的页面/功能点及每点的验收要点，是系统生成的直接依据。

**待定项**:
PRD 中仍缺用户确认信息的需求点；催促收敛或信息不足时缺口记录于此。

**生成**:
由对话发起（主智能体判断需求收敛时在对话区确认推进，或用户主动提出）后，平台自动在沙箱中起首个构建 run、产出系统初始形态；纯动作无门——待定项未清也可发起，之后的 PRD 变化走迭代。用户侧表述「正在为您生成系统」。
_Avoid_（用户可见面）: 开发、构建、出 Demo；定稿、确认门、G1（旧门概念已出局：PRD 无最终版一说，唯一冻结点是下单快照）

**迭代**:
首次生成后「用户在对话区提意见 → 主智能体受理（需求不清则追问；需求变更则改 PRD）→ 构建 run 按开发侧判定更新系统 → 系统更新」的循环，每一轮称一轮迭代。判定内化两侧：主智能体只判需求侧（要不要追问、要不要改 PRD），run 执行侧只判开发侧（要不要动系统、怎么动）；无需用户标注意见类型、无需逐条批准；迭代处理（受理或更新过程）进行中的新意见排队不丢失——受理在途的各自成轮，更新过程在途的下一轮合并处理。无次数上限，意见发散时主智能体催促收敛。
_Avoid_: 期、轮次回归、需求变更标记（显式标注口径已废止）、修正（用户面旧称，已改更新）

**成果区**:
项目界面的产物呈现区：tab 簇 + 呼出式文件区，与对话区并列为界面两大块。tab 簇是可扩展容器，「系统」（预览，默认主舞台）居首，其余位随体验设计增减（现含 PRD 文档位与订单信息位，均非固定结构）；文件区是工作区的窗户——PRD、代码、上传物料、导出物分目录同存——呼出可见、不常驻，入口标文件计数。
_Avoid_: 门户、工作台（旧壳称谓，已由对话区+成果区取代）、文件/系统/项目三模式（旧三模式口径已松绑）

**预览**:
成果区系统位对系统的实时呈现（沙箱内真实应用服务）；生成与更新 run 进行中即渐进呈现：应用可访问前以进行中的步骤提示（如「正在初始化」「正在创建首页」）占位，可访问后随编码进展逐步显现系统的长成；展示尽量停留在可正常呈现的状态，偶发的中间态报错如实呈现。
_Avoid_: demo 页、演示

**确认下单**:
用户宣告系统满意、进入下单流程的显式按钮动作；首次生成完成后常驻对话区，允许零迭代直接确认。
_Avoid_（用户可见面）: 验收、确认门、G2

**订单**:
用户确认下单后进入的交易载体：冻结下单时 PRD 快照、随源码包交后台。状态单向推进：待报价 → 已报价 → 已支付 → 已归档，另有已取消（取消即回迭代）；改价仅限已报价态，同项目至多一个未终结订单。
_Avoid_: 工单、交易单

**报价**:
后台对订单人工给出的总价与文本备注；已报价后可改价，改价留痕（只追加不改写）。
_Avoid_: 估价、计费（那是平台成本的说法）

**支付**:
用户对已报价订单的付款动作；支付成功即订单落「已支付」真实中间态，订单归档与项目归档是支付后的独立步骤（归档失败不抹支付事实）。v1 以平台内模拟支付走通全流程，真实渠道接入为实施末段切片。
_Avoid_: 收银、结算

**后台**:
平台运营方处理报价与改价的工作面，经机机签名接口与平台交互。
_Avoid_: admin、管理后台（旧 /admin 已出局）

**知识库**:
平台积累的跨项目共享知识，内容为成交项目的 PRD；全局共享、跨项目命中，项目删除时随之清理。
_Avoid_: RAG 库、向量库

**知识沉淀**:
订单归档（支付完成）时，平台将该项目的 PRD（取归档时最新版）入库知识库的动作。唯一沉淀触发点。
_Avoid_: 入库、索引

**知识命中**:
智能体切入之初——主智能体会话建立时、生成与更新 run 下发时——平台以当前需求检索知识库、把相似条目注入该智能体的机制。一次切入一次注入；检索失败降级为不注入。
_Avoid_: RAG 注入、检索增强

## 平台内部语言

**沙箱**:
系统生成与运行所在的隔离容器：平台置备、可随时销毁重建，重建后由工作区自愈；除工作区外不承载任何持久状态。
_Avoid_: 环境（旧词）、dev 容器

**工作区**:
一个项目唯一的持久化根：静态文件、docker 文件映射、系统文件与数据的全部落点；数据目录映射其中，容器可随时重建而不丢数据。面向用户不直接暴露此词。
_Avoid_: 沙箱目录、项目空间

**派发**:
平台把对话区输入安排为智能体动作序列的机制：编排归平台代码；语义判定下放两侧——主智能体判需求侧（追问/改 PRD/推进）、run 执行侧判开发侧（要不要动系统、怎么动），判定返回机器可读结果，编排按结果分岔——继续、跳过或终止，非固定流水线；入口车道判定（意见 / 咨询 / 兜底）为轻量实现细节。咨询类输入零产物短路、随时直接作答（挂起问答与订单冻结只拦意见链；对话区关闭即全停）。对用户全程隐式——用户不选择、不指派智能体，过程仅以阶段状态提示呈现。
_Avoid_: 智能体路由、用户指派、手动触发

**交接物**:
平台在需求侧收口后交给更新 run 的任务载体：意见原文（本轮与排队合并各条）、主智能体的需求侧判定结果（PRD 改没改、改了什么）、PRD 引用。意见链与更新链的交接只经此物；判定结果以平台可观测的事实为准，不由模型自报。
_Avoid_: 修正任务文本、prompt（实现词）

**运行（run）**:
构建执行的过程单位（生成 run / 更新 run）：执行体在沙箱工作区内读写代码、运行命令，把 PRD 变为系统、把用户意见变为系统更新；过程为五段循环——解读、探索、修改、自检、收口（自检现为最简探活收口，自测子智能体为首位升级候选）。run 完成信号驱动预览刷新与工作消息定格（收尾卡）。
_Avoid_: 任务（旧概念已出局）、会话、编码智能体（旧职能体词条已出局，其能力即 run 执行侧）、修正 run（旧称，已改更新 run）

**子智能体**:
run 执行体在运行中委派的专项职能智能体（自测、审查等）：任务自包含、结果回交执行体，不直接面对用户；事件带来源归属，过程呈现可分角色播。委派是 run 内机制，不与主智能体并列。
_Avoid_: 子代理、专家智能体、群聊

**技能**:
智能体按需加载的能力包：写作规范、工具组与知识的组合，承载可独立演进的过程知识（不能只靠角色卡 prompt）。来源三途：内置、自制、安装（市场技能，扩展点）。首个技能为 PRD 写作（内容即七章节模板）。
_Avoid_: 插件、扩展包、提示词包

**智能体事件**:
智能体运行过程的增量事件，对话区过程呈现与问答的呈现源；过程呈现采用消息部件模型（行业 parts 同构：解说文本部件 + 工具动作部件，带状态与时间戳），思考与代码补丁不作呈现。
_Avoid_: 智能体流事件（旧称）、帧（已全面改称事件）、平台通知

**平台通知**:
平台状态变化（生成开始、run 完成、订单状态变化等）的广播信号；只作实时呈现，状态以查询为准。
_Avoid_: 智能体事件、通知中心

**账号**:
平台内的用户档案，首次登录按外部身份自动建档；v1 无角色概念。
_Avoid_: 角色、权限组

**单价表**:
平台成本换算用的单价数据（模型 × token 档位 × 币种，带生效区间）；改价为关旧行开新行，历史成本不漂移。
_Avoid_: 价格表、费率（暗示售价）

**平台成本**:
平台为智能体模型调用付出的成本金额（token 用量 × 事件时点生效单价，按币种分桶）；运营口径，不含报价售价。
_Avoid_: 费用、计费
